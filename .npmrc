# @angular/fire's peer-dependency range currently pins an exact Angular 20.0.x
# patch (platform-browser-dynamic), while this project tracks the latest 20.x.
# The library itself works fine across the 20.x line — this just stops npm's
# strict peer resolution from failing `npm install` over the patch mismatch.
legacy-peer-deps=true
